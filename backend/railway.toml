[build]
builder = "nixpacks"

[deploy]
startCommand = "node src/infra/db/migrate.js && node src/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
